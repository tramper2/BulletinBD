{
  "name": "bulletin-bd-tester",
  "version": "1.0.0",
  "description": "Artractive Bulletin Board integration and test dashboard",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.2.11"
  }
}
